import FoundationLessonPage, { foundationLessonMetadata } from "@/components/FoundationLessonPage";

export const metadata = foundationLessonMetadata("docker-servicios");

export default function Page() {
  return <FoundationLessonPage slug="docker-servicios" />;
}
