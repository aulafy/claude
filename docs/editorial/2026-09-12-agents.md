# Lote editorial: 2026-09-12 / agents

## Encargo y estado

Nueva leccion bilingue sobre agentes, herramientas y limites de autonomia.

Estado: borrador validado localmente; pendiente de revision editorial y tecnica humana.
Idioma de referencia: ES. EN se ha redactado a partir de la revision ES.

## Resultado

La leccion separa propuesta y accion, define permisos, presupuesto, paradas y registro, y propone un flujo ficticio para identificar duplicados sin borrar registros. Incluye casos ambiguos y una aprobacion por accion.

El riesgo se marca como `high`. No hay herramientas conectadas ni cambios de datos reales. Los recuadros de ejemplo mantienen color y fondo explicitos, y el test de contraste cubre `pre` y `code`.

## Comprobaciones

```sh
npm run book:validate
npm run test:living-book
git diff --check
```

Resultado: validacion correcta, 14 pruebas correctas y diff sin errores de whitespace.

Hashes de revision:

```text
ES 13352537f33061ca5e2d54f101a929d4ed8a8a9be22a4aa77bf43207ff82b6ff
EN 60b643e16690850c8eb1e4a85d473dceab3b4177c4f11e3daa057a29100aedd2
```

`translationFrom.en` queda vinculado a la revision ES. Las aprobaciones permanecen vacias.

## Pendientes

- Revision tecnica humana de permisos, presupuesto, repeticion de acciones y borrado.
- Definir evaluaciones antes de incorporar un agente o proveedor real.
- QA de navegador tras recompilar para revisar la nueva ruta y el contraste de los recuadros.
