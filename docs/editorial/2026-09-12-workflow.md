# Lote editorial: 2026-09-12 / workflow

## Encargo y estado

Nueva leccion bilingue sobre convertir una tarea repetida en un flujo controlado.

Estado: borrador validado localmente; pendiente de revision editorial y tecnica humana.
Idioma de referencia: ES. EN se ha redactado a partir de la revision ES.

## Resultado

La leccion separa entrada, preparacion, borrador, comprobacion, salida y accion externa. Introduce estados explicitos y una parada entre la redaccion y cualquier envio. Los tres casos de prueba son ficticios: datos conocidos, dato ausente y contradiccion.

El riesgo esta marcado como `technical` porque el tema entra en automatizacion, aunque no se ejecuta ninguna integracion. El capitulo insiste en conservar entradas, evitar duplicar acciones con reintentos y empezar por una salida reversible.

## Comprobaciones

```sh
npm run book:validate
npm run test:living-book
git diff --check
```

Resultado: validacion correcta, 14 pruebas correctas y diff sin errores de whitespace.

Hashes de revision:

```text
ES 2e23ccea0ce6a9a33d572328d2ca93d9a66e736bdb64259eb399002071a57de1
EN b4c4b60270a6fc346f131ee849fad96e549dc05c370dddcd5f45b9156e44a640
```

`translationFrom.en` queda vinculado a la revision ES. Las aprobaciones permanecen vacias.

## Pendientes

- Revision tecnica humana de la explicacion de reintentos, duplicados y acciones externas.
- Confirmar que el lector entiende la diferencia entre guardar un borrador y enviarlo.
- Mantener esta leccion independiente de n8n, APIs o proveedores concretos hasta redactar `connectors` con fuentes apropiadas.
