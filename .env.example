# Copia este archivo a .env.local y rellena tus claves.

# --- OBLIGATORIO: Groq (motor del chatbot) ---
# Consíguela gratis en https://console.groq.com/keys
GROQ_API_KEY=
# Modelo de Groq (opcional, por defecto llama-3.3-70b-versatile)
GROQ_MODEL=llama-3.3-70b-versatile

# --- OPCIONAL: Upstash Redis (límite de peticiones por IP) ---
# Si las dejas vacías, el chatbot funciona igual pero SIN límite por IP.
# Crea una base de datos gratis en https://console.upstash.com (Redis)
# y copia las credenciales "REST".
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# --- AULAFY COMUNIDAD: Supabase (obligatorio para cuentas y proyectos) ---
# Copia la URL y la publishable key desde Project Settings > API.
# No uses aquí la service_role key.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# Actívalo solo después de configurar el proveedor OAuth en Supabase y Google.
NEXT_PUBLIC_SUPABASE_GOOGLE_ENABLED=false

# Solo desarrollo local: muestra proyectos ficticios para revisar la interfaz.
# Nunca se activa en producción aunque su valor sea true.
AULAFY_SOCIAL_PREVIEW=false

# --- SITIO Y VERIFICACIÓN (opcionales) ---
# Si se deja vacío, la aplicación usa https://www.aulafy.net como URL canónica.
# NEXT_PUBLIC_SITE_URL=https://www.aulafy.net
# Tokens públicos de verificación de buscadores.
# NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION=
# NEXT_PUBLIC_BING_SITE_VERIFICATION=
